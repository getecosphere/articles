{
  "name": "articles-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone Astro SSR UI for the articles domain: /articles/ and /articles/[slug]/ with SEO + OpenGraph, bun-compiled into a self-contained binary.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/node": "^8.3.2",
    "astro": "^4.15.0"
  }
}
